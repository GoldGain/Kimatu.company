// src/pages/dashboard/parent/Child.tsx
// Re-exports the existing Children (My Children) page under the /parent/child route
// so that App.tsx import resolves correctly.
export { default } from './Children';
